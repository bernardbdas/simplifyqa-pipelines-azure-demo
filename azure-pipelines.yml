trigger:
- main

resources:
  repositories:
    - repository: self

stages:
- stage: Build
  jobs:
    - template: matrix-template.yml
      parameters:
        matrix: ${{ fromYaml(file('matrix.yml')).matrix }}
